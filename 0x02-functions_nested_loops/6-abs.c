#include "main.h"

/**
 * _abs- prints the absolute value
 * @Num: Number
 * Return: success
 */

int _abs(int Num)
{
	if (Num < 0)
	{
		Num = (-1) * Num;
		return (Num);
	}
	else
	{
		return (Num);
	}
}
