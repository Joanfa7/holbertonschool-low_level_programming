#include <stdio.h>

/**
 * main- Program that print the Hexnumbers
 * Return: success
 */

int main(void)i

{
	int Num;

	for (Num = 48; Num <= 57; Num++)
	{
		putchar(Num);
	}
	for (Num = 97; Num <= 102; Num++)
	{
		putchar(Num);
	}

	putchar('\n');
	return (0);
}

